"""Reusable presentation primitives: shell, button, card, label.

Phase 1 scope. These are the only building blocks feature pages should
use to construct UI — no page should redefine colours, fonts, spacing,
or border/shadow behaviour inline. Everything traces back to
ui/tokens.py.
"""
